# Прокси для Российского сервера (Порт 7681)
server {
    listen 80;
    server_name servers.russian.localhost;

    location / {
        proxy_pass http://127.0.0.1:7681;
        proxy_http_version 1.1;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Прокси для Французского сервера (Порт 7682)
server {
    listen 80;
    server_name servers.france.localhost;

    location / {
        proxy_pass http://127.0.0.1:7682;
        proxy_http_version 1.1;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# Прокси для Китайского сервера (Порт 7683)
server {
    listen 80;
    server_name servers.chine.localhost;

    location / {
        proxy_pass http://127.0.0.1:7683;
        proxy_http_version 1.1;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}